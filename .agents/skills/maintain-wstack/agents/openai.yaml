interface:
  display_name: "Maintain wstack"
  short_description: "Plan, verify, and prepare wstack releases"
  default_prompt: "Use $maintain-wstack to make this repository change release-ready."
policy:
  allow_implicit_invocation: true
